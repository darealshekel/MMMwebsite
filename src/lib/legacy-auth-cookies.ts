// Legacy cookie names are intentionally preserved so existing browser sessions
// and deployed API auth contracts keep working after the MMM rebrand.
export const LEGACY_CSRF_COOKIE = "aetweaks_csrf";
